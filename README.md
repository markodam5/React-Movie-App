# Movie App

A React movie browsing app built with Vite. Users can search through a curated movie list and save favorites with persistent local storage.
Movie App is a lightweight front-end project focused on core React patterns: reusable components, client-side routing, controlled forms, and state persistence with `localStorage`.
